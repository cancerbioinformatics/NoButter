% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotTranscriptsByFOV.R
\name{PlotTranscriptsByFOV}
\alias{PlotTranscriptsByFOV}
\title{Visualize Transcripts for Fields of View (FOVs)}
\usage{
PlotTranscriptsByFOV(
  raw_transcripts,
  fov,
  fig.save = FALSE,
  out_dir = NULL,
  fig.width = 8,
  fig.height = 6
)
}
\arguments{
\item{raw_transcripts}{A data frame containing raw transcript data, including coordinates and FOV identifiers.}

\item{fov}{A vector of FOV identifiers for which the plots should be generated.}

\item{fig.save}{A logical value indicating whether to save the generated plots as PNG files. Defaults to \code{FALSE}.}

\item{out_dir}{A string specifying the directory where the plots should be saved if \code{fig.save} is \code{TRUE}. Defaults to \code{NULL}.}

\item{fig.width}{A numeric value specifying the width of the saved plot in inches. Defaults to 8.}

\item{fig.height}{A numeric value specifying the height of the saved plot in inches. Defaults to 6.}
}
\value{
Generates and optionally saves scatter plots of transcript data for each specified FOV.
}
\description{
This function generates scatter plots of transcript data for specified fields of view (FOVs) and optionally saves the plots as PNG files.
}
\details{
This function filters the raw transcript data to include only the specified FOVs, then generates scatter plots of the transcript coordinates. If \code{fig.save} is set to \code{TRUE}, the plots are saved to the specified output directory.
}
\examples{
\dontrun{
# Example usage:
raw_transcripts <- read.csv("path/to/raw_transcripts.csv")
fov_list <- c("FOV1", "FOV2", "FOV3")
PlotTranscriptsByFOV(raw_transcripts, fov_list, fig.save = TRUE, out_dir = "path/to/output/directory")
}
}
